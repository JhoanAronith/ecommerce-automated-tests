{
  "name": "ecommerce-tests",
  "version": "1.0.0",
  "description": "API ficticia para pruebas automatizadas",
  "scripts": {
    "api": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}